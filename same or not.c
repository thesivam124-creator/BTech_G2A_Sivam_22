#include<stdio.h>
int main() {
    int a,b;
    printf("enter the two numbers");
    scanf("%d%d",&a,&b);
    if(a==b)
    {printf("the numbers entered by the user is same");}
    else
    {
        printf("the numbers entered by the user are not same");
    }

}
    
    
